// tests.cpp : Defines the entry point for the console application.
//
#include <mindtriscore\mindtriscore.h>
#include <iostream>
#include <fstream>
using namespace std;


int main(int argc, char ** argv)
{
	/*BYTEARRAY Message;
	ofstream myfile;
    myfile.open ("hellofromclient.txt");

	DGMTProtocol protocol(true);
	Message = protocol.SEND_DGMT_HELLOFROMCLIENT();
    myfile << string(Message.begin(),Message.end());
	myfile.close();

	myfile.open ("hellofromserver_connected.txt");
	Message = protocol.SEND_DGMT_HELLOFROMSERVER(DGMTProtocol ::SHELLO_CONNECTED,"Hello!");
    myfile << string(Message.begin(),Message.end());
	myfile.close();

	myfile.open ("hellofromserver_wrong.txt");
	Message = protocol.SEND_DGMT_HELLOFROMSERVER(DGMTProtocol ::SHELLO_REFUSEDWRONGVERSION,"lol");
    myfile << string(Message.begin(),Message.end());
	myfile.close();

	myfile.open ("hellofromserver_unknownerror.txt");
	Message = protocol.SEND_DGMT_HELLOFROMSERVER(DGMTProtocol ::SHELLO_REFUSEDUNKNOWNERROR,"you suck");
    myfile << string(Message.begin(),Message.end());
	myfile.close();*/
	return 0;
}

